<form method="post" action="{{ path('palmares_delete', {'id': palmare.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ palmare.id) }}">
    
    <button  class="delete-btn"><i class="fas fa-trash-alt red-text"></i></button>
</form>
